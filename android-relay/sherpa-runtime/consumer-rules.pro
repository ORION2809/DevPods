# Add project specific ProGuard rules here.
# You can control the set of applied configuration files using the
# proguardFiles setting in android.buildTypes.
#
-keepclassmembers class com.openclaw.relay.sherpa.** {
    *;
}
-keepnames class com.openclaw.relay.sherpa.** { *; }