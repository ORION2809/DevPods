<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true">
        <receiver
            android:name=".RelayDebugReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.openclaw.relay.debug.RELAY_ACTION" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".DebugProofRunnerReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.openclaw.relay.action.RUN_PROOF" />
                <action android:name="com.openclaw.relay.action.EXPORT_PROOF" />
            </intent-filter>
        </receiver>
    </application>
</manifest>